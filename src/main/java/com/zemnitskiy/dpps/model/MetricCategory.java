package com.zemnitskiy.dpps.model;

/** Selects which metric sections (general, value, datetime) to include in the statistics response. */
public enum MetricCategory {
    GENERAL,
    VALUE,
    DATETIME
}
